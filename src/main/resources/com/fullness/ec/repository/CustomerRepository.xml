<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="com.fullness.ec.repository.CustomerRepository">
    <select id="selectByMailAddress" resultType="Customer" parameterType="String">
        select mail_address as mailAddress,username,password from customer where mail_address=#{Param1}
    </select>

    <update id="update" parameterType="ProductStock">
       update
           product_stock set
           quantity=#{quantity}
       where
           product_id=#{productId}  
    </update>    

</mapper>